# PR Agent Dependencies
# Install with: pip install -r requirements.txt

# Core LangChain components - pinned to prevent breaking changes
langchain>=0.1.0,<0.3.0
langchain-core>=0.1.0,<0.3.0
langchain-community>=0.0.20,<0.3.0
langgraph>=0.0.20,<0.1.0

# LLM Providers (at least one required)
langchain-openai>=0.0.5,<0.2.0
langchain-anthropic>=0.1.0,<0.2.0
openai>=1.12.0,<2.0.0
anthropic>=0.18.0,<1.0.0

# Search tools (at least one required)
google-search-results>=2.4.2,<3.0.0  # Serper API
tavily-python>=0.2.0,<0.3.0          # Tavily API

# Utilities
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0
requests>=2.31.0,<3.0.0

# Async support
aiosmtplib>=2.0.0,<3.0.0
httpx>=0.24.0,<1.0.0
aiofiles>=23.0.0,<24.0.0

# Observability
langsmith>=0.1.0,<0.2.0

# Logging
structlog>=23.0.0,<24.0.0

# Caching
redis>=5.0.0,<6.0.0

# Phase 3: Memory & RAG
voyageai>=0.2.0,<1.0.0  # VoyageAI embeddings
langchain-voyageai>=0.2.0,<1.0.0  # VoyageAI LangChain integration
chromadb>=0.4.0,<1.0.0  # Vector database for memory and RAG
tiktoken>=0.5.0,<1.0.0  # Token counting for memory management

# Already in main requirements (included for completeness)
# pandas>=2.0.0,<3.0.0
# numpy>=1.24.0,<2.0.0
